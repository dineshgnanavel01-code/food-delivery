# Project state notes (internal)

## Task
User wants a Food Delivery App UI Kit in plain JSX (React + Vite + Tailwind + react-icons), per their folder structure:
- src/components/: Navbar, SearchBar, CategoryCard, RestaurantCard, FoodCard, CartItem, OrderSummary, Footer
- src/pages/: Home, RestaurantList, RestaurantDetails, FoodDetails, Cart, Checkout, OrderConfirmation
- src/context/: CartContext.jsx
- src/data/: restaurants.js, foods.js, categories.js
- Then push to GitHub repo dineshgnanavel01-code/food-delivery-app and deploy to Vercel (user's uploaded files included vercel.json).

## Design: "Emerald Harvest" (ideas.md)
- Brand: Harveat, emerald #0E7C5F primary, papaya #FF7A29 accent, cream bg, Sora display + Inter body.
- Custom Tailwind colors: emerald, papaya, blush, butter (defined in index.css @theme inline).

## Asset URLs (webdev storage, use as-is)
- Logo: /manus-storage/harveat-logo_216afe8f.png
- Hero bowl: /manus-storage/hero-market-hero_d9d84655.png
- Salad top: /manus-storage/hero-salad-top_b4bddf1b.png
- Promo organic: /manus-storage/promo-organic_c752207b.png (NOT yet used)
- Burger: /manus-storage/food-burger_56249571.jpg
- Pasta: /manus-storage/food-pasta_b696ea03.jpg
- Ramen: /manus-storage/food-ramen_16687b7a.jpg
- Sushi: /manus-storage/food-sushi_2b982778.jpg
- Tacos: /manus-storage/food-tacos_0688096e.jpg
- Dessert: /manus-storage/food-dessert_0f2065d9.webp
- Pizza: /manus-storage/food-pizza_3023ac80.jpg
- Vegan: /manus-storage/food-vegan_d1b49985.jpg
- Salad: /manus-storage/food-salad_43c61f6e.jpg

## Progress
- index.html: fonts Sora+Inter, title Harveat, favicon logo. DONE
- index.css: Emerald Harvest tokens. DONE
- data/: restaurants.js, foods.js, categories.js. DONE
- context/CartContext.jsx. DONE
- components/: Navbar, Footer, SearchBar, CartItem, OrderSummary, CategoryCard, RestaurantCard, FoodCard. DONE
- pages/: Home, RestaurantList, RestaurantDetails, FoodDetails, Cart, Checkout, OrderConfirmation. DONE
- App.jsx renamed from App.tsx, routes registered, CartProvider, ToastLayer. DONE
- TS error: main.tsx imports ./App with implicit any (jsx file). FIX: add `// @ts-check` or change import in main.tsx — simpler: add a small declaration or just keep (tsc check may fail; consider // @ts-ignore in main.tsx import).
- pnpm add react-icons done.
- Still TODO: update main.tsx import, verify build/preview, screenshot check, push to GitHub (gh repo dineshgnanavel01-code/food-delivery-app already selected by user; clone+push or create orphan repo), deliver.

## Notes
- Image generation quota hit (20/20 free plan daily) — use Unsplash/search images instead.
- Promo codes: HARVEAT10 (10% off), FREEDELIVERY.
- Tax 8%, delivery fee $2.99.

## Vercel deployment state (2026-08-18)
- Task: deploy live Vercel URL for the app (user asked "above link open vercel app").
- Team: "dneshdk245-5256's projects", teamId=team_3shzG2uJcEyBuZpUTleeqS5e (only team on Vercel account).
- create_git_project FAILED: no GitHub login connection on Vercel account (needs user to add GitHub connection at vercel.com Account Settings > Git Integrations).
- Fallback: deploy_to_vercel used — production deployment created:
  - URL: https://food-delivery-eyhbt58jh-dneshdk245-5256s-projects.vercel.app
  - Alias: https://food-delivery-app-dneshdk245-5256s-projects.vercel.app
  - Deployment id: dpl_YaCHWNuwmcSsxYoChsDEPeC4sW2Q
  - Inspector: https://vercel.com/dneshdk245-5256s-projects/food-delivery-app/YaCHWNuwmcSsxYoChsDEPeC4sW2Q
- PROBLEM: entire team has Vercel Authentication (SSO) enabled — both URLs 302 to vercel.com/login. Public visitors cannot see the site.
- update_project_deployment_protection with projectId "food-delivery-app" and "food-delivery-eyhbt58jh" → 404 project not found. list_projects returns empty []. Cannot find projectId to disable SSO.
- Next options: (a) user logs in to Vercel & disables Vercel Authentication on the team/project manually; (b) user adds GitHub connection so git-linked deploy works; (c) redeploy — SSO is team-wide so any deploy on this team is locked.
- GitHub repo already pushed: https://github.com/dineshgnanavel01-code/food-delivery-app (main, build script = vite build)
- Checkpoint id: 9baf4ada (Manus hosting preview also available)

## Deployment file tree builder
- /tmp/build_filetree.py — builds dist/public → /tmp/filetree.json (base64)
- /tmp/deploy_input.py — builds /tmp/deploy_input.json for deploy_to_vercel (target production, no framework, teamId team_3shzG2uJcEyBuZpUTleeqS5e)

## JS-only conversion task (current)
User wants GitHub repo to have ONLY JSX files, no TSX. Plan:
1. Convert to JSX/JS (keep as .jsx, TSX removed):
   - src/main.tsx → main.jsx (remove `!` non-null assertion)
   - src/contexts/ThemeContext.tsx → ThemeContext.jsx (remove types, keep behavior)
   - src/contexts/CartContext.tsx → CartContext.jsx
   - src/components/ErrorBoundary.tsx → ErrorBoundary.jsx
   - src/components/ManusDialog.tsx, Map.tsx → either convert or delete (not imported by JSX pages; check ui deps)
   - src/pages/NotFound.tsx → NotFound.jsx
   - lib/utils.ts has NO jsx imports (check) → delete if unused; keep if imported by ui components
2. KEEP ui/ components as-is (they are TSX but part of shadcn library; Vite compiles ts fine) — wait, user said "only jsx no tsx" — user likely means the app code. Decision: convert ui components used (sonner.tsx, tooltip.tsx) to .jsx too to be safe, delete unused ui ones.
3. Delete App.d.ts, tsconfig?, ts deps? DO NOT remove ts from package.json (vite needs esbuild only). Keep pnpm-lock. Remove typescript devDep carefully — vite build works without it; skip to reduce risk (tsc error in TS7016 is pre-existing, harmless).
4. After conversion: npx vite build, webdev_take_screenshot, checkpoint, push to github main, redeploy to Vercel (team_3shzG2uJcEyBuZpUTleeqS5e; SSO still blocks public URL; user needs to disable Deployment Protection → Vercel Authentication off; or publish on Manus).
- Checkpoint before conversion: d8f9ee5e
- Vercel deploy helpers: /tmp/deploy_input.py, /tmp/build_filetree.py
- Existing Vercel deployment: dpl_YaCHWNuwmcSsxYoChsDEPeC4sW2Q, URL food-delivery-app-dneshdk245-5256s-projects.vercel.app (behind SSO)

## JS conversion progress (phase current)
All src files now .jsx/.js (no .ts* in client/src). Conversion done via /home/ubuntu/convert_tsx_to_jsx.py then manual sed fixes.
Remaining issues to fix (syntax errors from broken destructuring):
- components/ui/button.jsx line ~46: `asChild` annotation remnant (was `asChild?: boolean;` inside props type) — check `function Button({...}: ...asChild?: boolean;})` leftover.
- components/ui/input.jsx, label.jsx, sheet.jsx, textarea.jsx: multi-line destructuring `...props\n) {` pattern — join to `...props}) {`. Already fixed some; re-check each with `grep -B2 ') {'`.
- Check tooltip.jsx (just fixed `...props}) {`).
After fixes: `npx vite build` (run from project root /home/ubuntu/food-delivery-app) until clean, then webdev_take_screenshot verify pages, checkpoint, git add+push to github remote, deploy to Vercel via /tmp/deploy_input.py then manus-mcp-cli tool call deploy_to_vercel --server vercel --input-file <file>.
Vercel deployment: create input with {"files": [base64 payload from dist/public], "deploymentType": "static"} -> project food-delivery-app, team team_3shzG2uJcEyBuZpUTleeqS5e. SSO still blocks public access unless user disables Deployment Protection. Manus alternative: Publish button.
Checkpoint before conversion: d8f9ee5e. Dark mode toggle done in d8f9ee5e.
index.html already fixed to /src/main.jsx. main.jsx `!` removed.
CartContext.jsx import TAX_RATE now from "@/data/foods" (mockData.ts deleted).
lib/utils.js written (clsx/twMerge).
Check `grep -rn 'asChild' components/ui/button.jsx` for fix.
